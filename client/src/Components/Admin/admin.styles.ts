import { stylesheet } from 'typestyle'

export const css = stylesheet({
    container: {
        display: 'flex',
        justifyContent: 'center',
        marginTop: '4rem'
    },
    input: {
        width: '40rem'
    },
    button: {
        marginLeft: '3rem'
    }
})